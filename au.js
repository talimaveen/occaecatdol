// Define the out object with an add property that is an array
let out = {
  add: []
};

// A sample element to be added
let t = "newItem";

// Add the element to the array
out.add.push(t);

// Log the result to see the updated array
console.log(out.add); // Output: ["newItem"]
